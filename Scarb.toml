[package]
name = "semaphore_starknet"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = ">=2.8.0"

[[target.starknet-contract]]
sierra = true
casm = true

[dev-dependencies]
starknet = ">=2.8.0"
snforge_std = "0.56.0"
